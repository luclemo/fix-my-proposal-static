a
	color inherit
	text-decoration none
	border-bottom 1px dotted
	transition-all()
	&:hover
		background lighten(lightBlue, 20)
	

// Buttons --------------------------
.btn-primary
	btn()
	
.btn-large
	btn-size()
	
.btn-small
	btn-size(1.375rem)
	